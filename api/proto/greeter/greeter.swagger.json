{
  "swagger": "2.0",
  "info": {
    "title": "proto/greeter/greeter.proto",
    "version": "version not set"
  },
  "schemes": [
    "http",
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/api/v1/greetings": {
      "post": {
        "operationId": "PostGreeting",
        "responses": {
          "200": {
            "description": "",
            "schema": {
              "$ref": "#/definitions/greeterResponse"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/greeterRequest"
            }
          }
        ],
        "tags": [
          "Greeter"
        ]
      }
    },
    "/api/v1/greetings/{id}": {
      "get": {
        "operationId": "Hello",
        "responses": {
          "200": {
            "description": "",
            "schema": {
              "$ref": "#/definitions/greeterResponse"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "type": "string"
          }
        ],
        "tags": [
          "Greeter"
        ]
      }
    }
  },
  "definitions": {
    "greeterGreeting": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "@inject_tag: json:\"json\" bson:\"_id,omitempty\""
        },
        "msg": {
          "type": "string"
        },
        "friendliness": {
          "type": "integer",
          "format": "int32"
        }
      }
    },
    "greeterRequest": {
      "type": "object",
      "properties": {
        "body": {
          "$ref": "#/definitions/greeterGreeting"
        }
      }
    },
    "greeterResponse": {
      "type": "object",
      "properties": {
        "body": {
          "$ref": "#/definitions/greeterGreeting"
        }
      }
    }
  }
}
